-- WARNING: This file was generated automatically by Vehicle
-- and should not be modified manually!
-- Metadata:
--  - Agda version: 2.6.2
--  - Vehicle version: 0.16.0+dev

{-# OPTIONS --allow-exec #-}

open import Vehicle
open import Vehicle.Utils
open import Vehicle.Data.Tensor
open import Data.Unit
open import Data.Empty
open import Data.Product
open import Data.Nat as ℕ using (ℕ)
open import Data.Integer as ℤ using (ℤ)
open import Data.Rational as ℚ using (ℚ)
open import Data.List.Base

module Agda where

ForallInList : ℕ → Set → List ℕ → Set
ForallInList f xs = List.foldr (λ x → λ y → x × y) ⊤ (List.map f xs)

postulate e1 : let x = ⊤ in let y = ⊥ in x × y

postulate e2 : let y = 1 in y ≥ 2

e3 : ℕ
e3 = let y = 1 ℕ.⊕ 1 in y

lamLet : ℕ → ℕ
lamLet = λ ((x : ℕ)) → let y = x in y

letLam : ℕ → ℕ
letLam = let y = λ x → x in y

postulate forallLet : ∀ ((x : Tensor ℚ [])) → let y = x in y ≈ 1

postulate letForall : let y = 1 in ∀ ((x : Tensor ℚ [])) → y ≈ x

postulate forallInLet : ForallInList (λ x → let y = x in y ≈ 1) (1 ∷ [])

postulate letForallIn : let y = 1 in ForallInList (λ x → y ≈ x) (1 ∷ [])

Falsey : Tensor ℚ [] → Set
Falsey x = x ≤ ℤ.+ 1 / 2

postulate correctOutput : let y = 0 in Falsey y